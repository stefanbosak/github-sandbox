name: 'Example for Linux'

on:
  workflow_dispatch:
    inputs:
      os:
        description: 'Runner label'
        required: true
        default: '["ubuntu-latest","ubuntu-24.04-arm"]'
        type: string

jobs:
  test:
    runs-on: ${{ matrix.runner }}
    strategy:
      matrix:
        runner: ${{ fromJson(inputs.os) }}

    steps:
      - name: 'Run task'
        shell: bash
        run: echo "Running arch=$(uname -m) runner_name=${{ runner.name }} on hostname=$(hostname)."
      - name: 'Start docker'
        shell: bash
        run: |
          id
          sudo /etc/init.d/docker restart
      - name: 'Run docker container'
        shell: bash
        run: |
          id
          docker system info
          docker container run --rm alpine/curl -fsSL https://ifconfig.me
